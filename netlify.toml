# Netlify build config for Valentine (Vite) app
# Ensures the built site (dist) is deployed, not the source files

[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
